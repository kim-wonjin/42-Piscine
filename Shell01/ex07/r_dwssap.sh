cat /etc/passwd | sed '/#/d' | awk '{if (NR%2==0) print}' | sed 's/:.*//' | rev | sort -r |  awk -v start=$FT_LINE1 -v end=$FT_LINE2 'NR==start,NR==end'| tr '\n' ',' | sed 's/,/, /g' | sed 's/, $/./' | tr -d '\n'
